#ifndef HEADER_H
#define HEADER_H
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
int _printf(char *,...);
char* convert(unsigned int, int);
int _putchar(char c);
void printnum(int n);
#endif /* HEADER_H */
